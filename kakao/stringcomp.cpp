#include <iostream>
#include <string>

using namespace std;

int solution(string str)
{
    int ans = 10001;
    int index = str.size() / 2;
    for(int x = index; x>0; x--)
    {
        string print = "";
        string compare = "";
        int count = 1;
        for(int i = 0; i< str.size(); i+= x)
        {
            string cur = str.substr(i,x);
            continue
        }
        g
    }
}
int main(){
    string t = "aaabbbccc";
}
